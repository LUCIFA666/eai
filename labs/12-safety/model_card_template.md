# Model Card — `{{model_id}}`

> Generated by `labs/12-safety/generate_model_card.py` on `{{generated_at}}`.
> Fill any `TODO:` field by hand; the template only auto-fills what the
> checkpoint metadata reliably contains.

## 1. Model details

- **Model name**: `{{model_name}}`
- **Version / checkpoint step**: `{{version}}` (step `{{train_step}}`)
- **Task**: `{{task}}`
- **Architecture**: `{{architecture}}` ({{n_params_m}}M params)
- **Input modalities**: {{input_modalities}}
- **Action space**: {{action_space}}
- **Control frequency**: {{control_hz}} Hz
- **License**: {{license}}
- **Authors / owners**: TODO: name + contact
- **Code commit**: `{{git_sha}}`

## 2. Intended use

- **Primary**: TODO: e.g. tabletop pick-and-place evaluation under static lighting.
- **Out-of-scope**: TODO: e.g. moving humans, deformables, real-world deployment without operator supervision.
- **User class**: researchers / supervised demos / lab evaluators.

## 3. Training data

- **Dataset**: `{{dataset_name}}` (version `{{dataset_version}}`)
- **Episodes**: {{n_train_episodes}} train / {{n_val_episodes}} val
- **Source environment(s)**: {{train_envs}}
- **Annotation pipeline**: TODO: teleop / scripted / human relabel.
- **Privacy review**: TODO: contains faces? voices? home layouts? screens?
- **Demographic notes**: TODO: operator diversity, lighting, object distribution.

## 4. Evaluation

- **Eval suite**: `{{eval_suite}}`
- **Metric**: {{metric_name}} = **{{metric_value}}** (95% CI {{metric_ci_lo}}-{{metric_ci_hi}}, n={{n_eval_episodes}}, {{n_seeds}} seeds)
- **Reference SOTA on this suite**: TODO.
- **Failure modes observed** (see `labs/11-eval/failure_taxonomy_template.csv`):
  - TODO: top-3 failure categories with counts.

## 5. Safety & limits

- **Operating envelope**: TODO: workspace box (x,y,z), max payload, max EE speed.
- **Required interlocks**: e-stop within reach, software torque cap, watchdog `<{{watchdog_ms}} ms`.
- **Known hazards**: TODO: e.g. fast closure on fingers, tipping payload.
- **Required operator**: trained, supervised; never autonomous overnight.

## 6. Reproducibility

- **Run manifest**: `{{manifest_path}}`
- **Python**: {{python_version}}  / **packages**: see `requirements.lock`
- **Hardware**: {{hardware}}
- **Seed(s)**: {{seeds}}
- **Eval command**:

```bash
{{eval_command}}
```

## 7. Ethical considerations

- **Data subjects**: TODO: any human subjects? IRB / consent reference?
- **Bias risk**: TODO: object-class imbalance, environment monoculture.
- **Misuse risk**: TODO: surveillance reuse, weaponisation.
- **NIST AI RMF mapping**: GOVERN / MAP / MEASURE / MANAGE rows — keep the mapping with the deployment notes for the relevant real-robot experiment.

## 8. Citation

```bibtex
@misc{{ {{citation_key}} ,
  title  = { {{model_name}} },
  author = { TODO },
  year   = { {{year}} },
  note   = { commit {{git_sha}} },
}
```
