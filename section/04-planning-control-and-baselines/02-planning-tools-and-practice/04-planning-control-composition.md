# 规划控制组合范式

本节介绍“全局规划 + 局部反应 + 时间参数化 + 控制执行”的组合范式：全局规划负责生成可行路径或粗轨迹，局部反应负责实时避障与目标跟随，时间参数化把几何路径转成满足速度、加速度和 jerk 约束的轨迹，控制执行负责把轨迹稳定下发到仿真或真机控制器。要写清 MoveIt 2、cuRobo、RMPflow、Ruckig、ros2_control / JointTrajectoryController 等组件在这条链路中的分工，而不是只做工具横向对比。
